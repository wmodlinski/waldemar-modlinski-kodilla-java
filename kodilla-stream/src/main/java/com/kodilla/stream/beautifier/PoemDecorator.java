package com.kodilla.stream.beautifier;

public interface PoemDecorator {
    //zawierający metodę decorate odpowiedzialną za ozdobienie
    //tekstu przekazanego jako parametr.

    String decorate(String text);
}
